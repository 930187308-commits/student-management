{
  "name": "student-ai-console",
  "version": "0.1.0",
  "private": true,
  "description": "Personal student management and AI education console.",
  "type": "commonjs",
  "scripts": {
    "start": "node server/server.js",
    "dev": "STUDENT_CONSOLE_ENV=development node server/server.js",
    "check": "node --check server/server.js && node --check server/db.js && node --check server/config.js"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
